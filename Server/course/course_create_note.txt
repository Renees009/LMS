Backend endpoint mismatch:
- Frontend AddCourse posts to POST /api/course/create/ with multipart form.
- Existing backend only has:
  - GET /api/courses/ (list)
  - GET /api/courses/<pk>/ (detail)
  - tutor course list /api/tutor-courses/
  - student me enrollments /api/me/enrollments/ and completions /api/me/completions/
- There is NO /api/course/create/ endpoint.

Need to implement:
- Instructor upload endpoint to create Course + TutorCourse mapping + Lessons + LessonContent (+ save material/video)

