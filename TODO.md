- [ ] Create plan for updating EnrolledCourse to show all enrolled course cards returned from DB/API
- [ ] Verify API endpoint /api/me/enrollments/ returns expected list payload shape
- [ ] Update client EnrolledCourse.jsx to render all enrolled courses (and handle pagination if DRF returns {count,results})
- [ ] Ensure CourseCard receives correct props (course vs enrollment.course) and unique keys
- [ ] Run frontend tests/build and verify UI rendering

