# TODO
- [ ] Update CourseCard "Enroll" button to navigate to `/course/:id` immediately after successful enrollment (remove/adjust any behavior that doesn’t navigate to course details).
- [ ] Ensure navigation uses the correct route and matches `CourseDetails` expectation (`/course/:courseId`).
- [ ] Run frontend build/lint or quick smoke test.

